import { Text, View } from "react-native";

type AnimalProps ={
    name:string,
    emoji:string
}

export default function AnimalCard({name , emoji}:AnimalProps){
    return(
        <View>
            <Text
        </View>
    )
}